const STARFIELD_CONFIG={count:window.matchMedia("(max-width: 767px)").matches?72:128,driftCount:window.matchMedia("(max-width: 767px)").matches?8:16,parallaxMax:2,parallaxLerp:0.06,};const STAR_COLORS=[{weight:70,rgb:[236,242,255]},{weight:20,rgb:[170,230,245]},{weight:10,rgb:[210,190,245]},];function pickStarColor(rand){const total=STAR_COLORS.reduce((sum,item)=>sum+item.weight,0);let ticket=rand()*total;for(const item of STAR_COLORS){ticket-=item.weight;if(ticket<=0)return item.rgb;}
return STAR_COLORS[0].rgb;}
function createRng(seed){let state=seed>>>0;return()=>{state=(state*1664525+1013904223)>>>0;return state/4294967296;};}
class NightSkyScene{constructor(panel,engine,config=STARFIELD_CONFIG){this.panel=panel;this.engine=engine;this.config=config;this.reduceMotion=window.matchMedia("(prefers-reduced-motion: reduce)").matches;this.isIntersecting=false;this.layoutDirty=true;this.stars=[];this.width=0;this.height=0;this.pointerX=0;this.pointerY=0;this.parallaxX=0;this.parallaxY=0;this.startTime=0;}
init(){this.canvas=document.createElement("canvas");this.canvas.className="night-sky";this.canvas.setAttribute("aria-hidden","true");this.ctx=this.canvas.getContext("2d",{alpha:true});this.panel.prepend(this.canvas);this.onPointerMove=(event)=>{const rect=this.panel.getBoundingClientRect();if(rect.width<=0||rect.height<=0)return;this.pointerX=((event.clientX-rect.left)/rect.width)*2-1;this.pointerY=((event.clientY-rect.top)/rect.height)*2-1;};if(window.matchMedia("(pointer: fine)").matches){this.panel.addEventListener("pointermove",this.onPointerMove,{passive:true});}
this.resizeObserver=new ResizeObserver(()=>{this.layoutDirty=true;this.engine.requestFrame();});this.resizeObserver.observe(this.panel);this.intersectionObserver=new IntersectionObserver(([entry])=>{this.isIntersecting=entry.isIntersecting;this.updateCompositing(this.engine.pageVisible);if(this.isIntersecting)this.engine.requestFrame();},{rootMargin:"40px",threshold:0});this.intersectionObserver.observe(this.panel);this.layout();this.engine.add(this);}
needsFrame(){return this.layoutDirty||this.isAnimating();}
isAnimating(){return this.isIntersecting&&!this.reduceMotion;}
updateCompositing(pageVisible){this.canvas.classList.toggle("is-active",pageVisible&&this.isIntersecting&&!this.reduceMotion);}
layout(){this.layoutDirty=false;const rect=this.panel.getBoundingClientRect();const width=Math.max(1,Math.floor(rect.width));const height=Math.max(1,Math.floor(rect.height));const isMobile=window.matchMedia("(max-width: 767px)").matches;const dpr=Math.min(window.devicePixelRatio||1,isMobile?1.25:1.5);if(width===this.width&&height===this.height&&this.stars.length){return;}
this.width=width;this.height=height;this.canvas.width=Math.floor(width*dpr);this.canvas.height=Math.floor(height*dpr);this.canvas.style.width=`${width}px`;this.canvas.style.height=`${height}px`;this.ctx.setTransform(dpr,0,0,dpr,0,0);this.buildStars();if(this.reduceMotion)this.renderStatic();}
buildStars(){const rand=createRng(20260729);const{count,driftCount}=this.config;const driftIndexes=new Set();while(driftIndexes.size<Math.min(driftCount,count)){driftIndexes.add(Math.floor(rand()*count));}
this.stars=[];for(let i=0;i<count;i+=1){const drifts=driftIndexes.has(i);const speed=drifts?0.2+rand()*0.6:0;const angle=rand()*Math.PI*2;this.stars.push({x:rand()*this.width,y:rand()*this.height,size:1+rand()*2,baseAlpha:0.35+rand()*0.55,color:pickStarColor(rand),period:2000+rand()*6000,phase:rand(),driftX:Math.cos(angle)*speed,driftY:Math.sin(angle)*speed,});}}
renderStatic(){const{ctx,width,height,stars}=this;ctx.clearRect(0,0,width,height);for(const star of stars){const alpha=star.baseAlpha*0.7;ctx.fillStyle=`rgba(${star.color[0]},${star.color[1]},${star.color[2]},${alpha.toFixed(3)})`;ctx.fillRect(star.x,star.y,star.size,star.size);}}
render(time){if(!this.startTime)this.startTime=time;const elapsed=(time-this.startTime)/1000;const{ctx,width,height,stars,config}=this;const max=config.parallaxMax;this.parallaxX+=(this.pointerX*max-this.parallaxX)*config.parallaxLerp;this.parallaxY+=(this.pointerY*max-this.parallaxY)*config.parallaxLerp;ctx.clearRect(0,0,width,height);for(const star of stars){let x=star.x+star.driftX*elapsed+this.parallaxX;let y=star.y+star.driftY*elapsed+this.parallaxY;x=((x%width)+width)%width;y=((y%height)+height)%height;const twinkle=0.5-0.5*Math.cos(((time/star.period)+star.phase)*Math.PI*2);const peak=0.85+star.baseAlpha*0.15;const alpha=0.2+(peak-0.2)*twinkle;ctx.fillStyle=`rgba(${star.color[0]},${star.color[1]},${star.color[2]},${alpha.toFixed(3)})`;ctx.fillRect(x,y,star.size,star.size);}}
destroy(){if(window.matchMedia("(pointer: fine)").matches){this.panel.removeEventListener("pointermove",this.onPointerMove);}
this.resizeObserver.disconnect();this.intersectionObserver.disconnect();this.engine.remove(this);this.canvas.remove();}}
function setupNightSkies(engine){const panels=Array.from(document.querySelectorAll("[data-night-sky]"));if(panels.length===0)return;const animEngine=engine||window.CampusAnim?.getEngine?.();if(!animEngine)return;for(const panel of panels){const sky=new NightSkyScene(panel,animEngine);sky.init();}}
window.CampusStarfield={setupNightSkies};const PARALLAX_CONFIG={refWidth:1280,refHeight:960,breathScale:0.045,particleCount:8,};const SCENE_LAYERS=[{id:"背景",src:"images/class_all/layers_rgba/背景.webp",left:0,top:0,width:100,depth:"background",z:1,moveX:0,moveY:0,period:11000,phase:0,},{id:"左壁_後",src:"images/class_all/layers_rgba/左壁_後.webp",left:30.1253,top:27.0106,width:21.5831,depth:"mid-back",z:2,moveX:-7,moveY:31,period:4300,phase:0.17,},{id:"右壁_後",src:"images/class_all/layers_rgba/右壁_後.webp",left:59.738,top:34.9772,width:26.2528,depth:"mid-back",z:3,moveX:6,moveY:25,period:4900,phase:0.39,},{id:"缶",src:"images/class_all/layers_rgba/缶.webp",left:33.8269,top:54.6282,width:8.713,depth:"mid-front",z:4,moveX:-9,moveY:38,period:5600,phase:0.61,},{id:"左壁",src:"images/class_all/layers_rgba/左壁.webp",left:0,top:4.7041,width:39.9203,depth:"wall",z:5,moveX:5,moveY:28,period:6200,phase:0.82,},{id:"右テーブル",src:"images/class_all/layers_rgba/右テーブル.webp",left:51.8223,top:59.8634,width:34.1686,depth:"foreground",z:6,moveX:-8,moveY:45,period:6800,phase:0.28,},{id:"右壁",src:"images/class_all/layers_rgba/右壁.webp",left:78.5308,top:0,width:21.4692,depth:"wall",z:7,moveX:6,moveY:34,period:7100,phase:0.5,},{id:"中央テーブル",src:"images/class_all/layers_rgba/中央テーブル.webp",left:39.9203,top:51.214,width:31.6059,depth:"foreground",z:8,moveX:-7,moveY:42,period:7500,phase:0.72,},];const TAU=Math.PI*2;function smoothWave(angle){return(1-Math.cos(angle))*0.5;}
class AnimationEngine{constructor(){this.scenes=[];this.frameId=0;this.pageVisible=!document.hidden;this.tick=this.tick.bind(this);this.onVisibilityChange=this.onVisibilityChange.bind(this);document.addEventListener("visibilitychange",this.onVisibilityChange);}
add(scene){this.scenes.push(scene);this.requestFrame();}
remove(scene){const index=this.scenes.indexOf(scene);if(index!==-1)this.scenes.splice(index,1);if(!this.hasWork()&&this.frameId){cancelAnimationFrame(this.frameId);this.frameId=0;}}
onVisibilityChange(){this.pageVisible=!document.hidden;for(const scene of this.scenes){if(typeof scene.updateCompositing==="function"){scene.updateCompositing(this.pageVisible);}}
if(!this.pageVisible&&this.frameId){cancelAnimationFrame(this.frameId);this.frameId=0;return;}
if(this.pageVisible)this.requestFrame();}
hasWork(){return this.pageVisible&&this.scenes.some((scene)=>scene.needsFrame());}
requestFrame(){if(!this.frameId&&this.hasWork()){this.frameId=requestAnimationFrame(this.tick);}}
tick(time){this.frameId=0;if(!this.pageVisible)return;for(const scene of this.scenes){if(scene.layoutDirty)scene.layout();if(scene.isAnimating())scene.render(time);}
if(this.hasWork())this.requestFrame();}}
window.CampusAnim=window.CampusAnim||{_engine:null,getEngine(){if(!this._engine)this._engine=new AnimationEngine();return this._engine;},};class ParallaxScene{constructor(root,engine,layers=SCENE_LAYERS,config=PARALLAX_CONFIG){this.root=root;this.engine=engine;this.layerSettings=layers;this.config=config;this.assetBase=root.dataset.assetBase||"";this.reduceMotion=window.matchMedia("(prefers-reduced-motion: reduce)").matches;this.isIntersecting=false;this.layoutDirty=true;this.worldHeight=0;this.layers=[];this.particles=[];}
init(){this.root.classList.add("parallax-scene");this.root.setAttribute("aria-hidden","true");this.world=document.createElement("div");this.world.className="parallax-scene__world";this.glow=document.createElement("div");this.glow.className="parallax-scene__glow";this.root.append(this.world,this.glow);this.buildLayers();this.buildParticles();this.bindObservers();this.layout();this.engine.add(this);}
buildLayers(){const fragment=document.createDocumentFragment();for(const setting of this.layerSettings){const slot=document.createElement("div");slot.className="parallax-scene__slot";slot.style.cssText=`left:${setting.left}%;top:${setting.top}%;width:${setting.width}%;z-index:${setting.z}`;const image=document.createElement("img");image.className="parallax-scene__layer";image.src=`${this.assetBase}${setting.src}`;image.alt="";image.draggable=false;image.decoding="async";image.loading=setting.depth==="background"?"eager":"lazy";image.dataset.depth=setting.depth;if(setting.depth==="background"){slot.classList.add("is-background");image.classList.add("is-background");}else{slot.classList.add("is-animated");}
slot.appendChild(image);fragment.appendChild(slot);this.layers.push({element:slot,setting});}
this.world.appendChild(fragment);}
buildParticles(){if(this.reduceMotion||this.config.particleCount<=0)return;const container=document.createElement("div");container.className="parallax-scene__particles";const fragment=document.createDocumentFragment();for(let i=0;i<this.config.particleCount;i+=1){const element=document.createElement("span");const size=7+(i%3)*4;const setting={duration:15000+(i%4)*3000,delay:(i*2700)%17000,drift:(i%2===0?1:-1)*(8+(i%3)*4),opacity:0.22+(i%3)*0.06,};element.className="parallax-scene__particle";element.style.cssText=`left:${8 + ((i * 37) % 84)}%;width:${size}px;height:${size}px;`+`margin-left:${size / -2}px`;fragment.appendChild(element);this.particles.push({element,setting});}
container.appendChild(fragment);this.root.appendChild(container);}
bindObservers(){this.resizeObserver=new ResizeObserver(()=>{this.layoutDirty=true;this.engine.requestFrame();});this.resizeObserver.observe(this.root);this.intersectionObserver=new IntersectionObserver(([entry])=>{this.isIntersecting=entry.isIntersecting;this.updateCompositing(this.engine.pageVisible);if(this.isIntersecting)this.engine.requestFrame();},{rootMargin:"40px",threshold:0});this.intersectionObserver.observe(this.root);}
needsFrame(){return this.layoutDirty||this.isAnimating();}
isAnimating(){return this.isIntersecting&&!this.reduceMotion;}
updateCompositing(pageVisible){this.root.classList.toggle("is-active",pageVisible&&this.isIntersecting&&!this.reduceMotion);}
layout(){this.layoutDirty=false;const{width:sceneWidth,height:sceneHeight}=this.root.getBoundingClientRect();if(sceneWidth<=0||sceneHeight<=0)return;const referenceRatio=this.config.refWidth/this.config.refHeight;const sceneRatio=sceneWidth/sceneHeight;let worldWidth=sceneRatio>referenceRatio?sceneWidth:sceneHeight*referenceRatio;let worldHeight=sceneRatio>referenceRatio?sceneWidth/referenceRatio:sceneHeight;this.worldHeight=worldHeight;this.world.style.width=`${worldWidth}px`;this.world.style.height=`${worldHeight}px`;this.world.style.left=`${(sceneWidth - worldWidth) / 2}px`;this.world.style.top=`${(sceneHeight - worldHeight) / 2}px`;}
render(time){for(const layer of this.layers){const{element,setting}=layer;const angle=(time/setting.period+setting.phase)*TAU;if(setting.depth==="background"){const scale=1+smoothWave(angle)*this.config.breathScale;element.style.transform=`translate3d(0,0,0) scale(${scale.toFixed(4)})`;continue;}
const x=Math.sin(angle)*setting.moveX;const y=-smoothWave(angle)*setting.moveY;element.style.transform=`translate3d(${x.toFixed(2)}px,${y.toFixed(2)}px,0)`;}
const particleTravel=this.worldHeight+120;for(const particle of this.particles){const{element,setting}=particle;const progress=((time+setting.delay)%setting.duration)/setting.duration;const eased=progress*progress*(3-2*progress);const x=setting.drift*Math.sin(progress*Math.PI);const y=-particleTravel*eased;const scale=0.7+progress*0.45;const fadeIn=Math.min(1,progress/0.12);const fadeOut=Math.min(1,(1-progress)/0.3);element.style.transform=`translate3d(${x.toFixed(2)}px,${y.toFixed(2)}px,0) scale(${scale.toFixed(3)})`;element.style.opacity=(setting.opacity*fadeIn*fadeOut).toFixed(3);}
const glowAngle=(time/22000)*TAU;const glowX=Math.sin(glowAngle)*3;const glowY=Math.cos(glowAngle)*2;this.glow.style.transform=`translate3d(${glowX.toFixed(2)}%,${glowY.toFixed(2)}%,0) scale(1.08)`;}
destroy(){this.resizeObserver.disconnect();this.intersectionObserver.disconnect();this.engine.remove(this);}}
function setupParallaxScenes(){const engine=window.CampusAnim.getEngine();const roots=Array.from(document.querySelectorAll("[data-parallax-scene]"));const mobileQuery=window.matchMedia("(max-width: 767px)");const scenes=new Map();function syncScenes(){for(const root of roots){if(mobileQuery.matches){const scene=scenes.get(root);if(scene){scene.destroy();scenes.delete(root);}
root.replaceChildren();root.hidden=true;}else if(!scenes.has(root)){root.hidden=false;const scene=new ParallaxScene(root,engine);scenes.set(root,scene);scene.init();}}}
if(roots.length>0){syncScenes();mobileQuery.addEventListener("change",syncScenes);}
if(window.CampusStarfield?.setupNightSkies){window.CampusStarfield.setupNightSkies(engine);}}
function bootCampusMotion(){setupParallaxScenes();}
if(document.readyState==="loading"){document.addEventListener("DOMContentLoaded",bootCampusMotion,{once:true});}else{bootCampusMotion();}
document.addEventListener("DOMContentLoaded",()=>{setupStickyHeader();setupEventCarousel();});function setupStickyHeader(){const header=document.querySelector(".site-header");if(!header)return;const syncOffset=()=>{document.documentElement.style.setProperty("--header-offset",`${header.offsetHeight}px`);};const updateScrolled=()=>{header.classList.toggle("is-scrolled",window.scrollY>8);};syncOffset();updateScrolled();window.addEventListener("scroll",updateScrolled,{passive:true});window.addEventListener("resize",syncOffset);}
function setupEventCarousel(){const root=document.querySelector("[data-event-carousel]");if(!root)return;const list=root.querySelector(".event-list");if(!list)return;const items=Array.from(list.children);const count=items.length;if(count===0)return;const prevBtn=root.querySelector("[data-carousel-prev]");const nextBtn=root.querySelector("[data-carousel-next]");const INTERVAL_MS=7000;const TRANSITION_MS=900;const reduceMotion=window.matchMedia("(prefers-reduced-motion: reduce)").matches;const step=360/count;let index=0;let rotationDeg=0;let timerId=null;let animating=false;let pendingDir=0;function radius(){const refW=220;const base=Math.max(260,Math.round(refW/(2*Math.tan(Math.PI/count))));const desktop=base*1.5*1.5;const isMobile=window.matchMedia("(max-width: 767px)").matches;return isMobile?Math.round(desktop*0.52):desktop;}
function layout(animate){const r=radius();list.style.transition=animate&&!reduceMotion?`transform ${TRANSITION_MS}ms ease`:"none";list.style.transform=`translateZ(${-r}px) rotateY(${rotationDeg}deg)`;items.forEach((li,i)=>{const angle=i*step;li.style.transform=`rotateY(${angle}deg) translateZ(${r}px)`;let diff=Math.abs(i-index);diff=Math.min(diff,count-diff);li.style.zIndex=String(count-diff);const card=li.querySelector(".event-card");if(card){card.classList.toggle("is-active",diff===0);}});}
function applyStep(dir){index=(index+dir+count)%count;rotationDeg-=dir*step;layout(true);}
function goNext(){applyStep(1);}
function goPrev(){applyStep(-1);}
function finishAnim(){animating=false;if(pendingDir!==0){const dir=pendingDir;pendingDir=0;runStep(dir);}}
function runStep(dir){if(reduceMotion){applyStep(dir);return;}
if(animating){pendingDir=dir;return;}
animating=true;applyStep(dir);window.setTimeout(finishAnim,TRANSITION_MS);}
function startAuto(){stopAuto();if(reduceMotion)return;timerId=window.setInterval(()=>{runStep(1);},INTERVAL_MS);}
function stopAuto(){if(timerId!==null){window.clearInterval(timerId);timerId=null;}}
function onManual(dir){runStep(dir);startAuto();}
if(prevBtn){prevBtn.addEventListener("click",(e)=>{e.preventDefault();e.stopPropagation();onManual(-1);});}
if(nextBtn){nextBtn.addEventListener("click",(e)=>{e.preventDefault();e.stopPropagation();onManual(1);});}
window.addEventListener("resize",()=>layout(false));document.addEventListener("visibilitychange",()=>{if(document.hidden)stopAuto();else startAuto();});layout(false);startAuto();}